# Admin model for MongoDB (for reference, not required by ODM)
# Admins will be stored in the 'admins' collection as {username, password_hash}
# No additional model file needed since we use direct dicts.
